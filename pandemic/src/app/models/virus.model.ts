export class Virus {
    nombre: string;
    color: string;
    id: number;
  
    constructor(id: number, nombre: string, color: string) {
      this.id = id;
      this.nombre = nombre;
      this.color = color;
    }
}